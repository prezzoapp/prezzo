import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btn: {
    padding: 3
  },

  ratingBarContainer: {
    flexDirection: 'row'
  },

  starImage: {
    width: 20,
    height: 20,
    resizeMode: 'cover',
    tintColor: '#FFD64D'
  }
});

export default styles;
